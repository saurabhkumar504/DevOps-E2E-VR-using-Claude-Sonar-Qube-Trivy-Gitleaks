# =====================================================================
# OWASP VULNERABILITY LEARNING LAB
# WARNING: ALL VALUES BELOW ARE INTENTIONALLY INSECURE.
# DO NOT DEPLOY. EDUCATIONAL USE ONLY.
# =====================================================================

# Application
spring.application.name=vulnerable-spring-app
server.port=8080

# ---------------------------------------------------------------------
# VULNERABILITY (OWASP A05:2021 - Security Misconfiguration /
#                OWASP A02:2021 - Cryptographic Failures):
# Hardcoded DB credentials committed to source. Production must use
# a secrets manager / environment variables / vault.
# ---------------------------------------------------------------------
spring.datasource.url=jdbc:h2:mem:owaspdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# VULNERABILITY: hardcoded "secret" key in source code (A02:2021 / A05:2021)




# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# VULNERABILITY: enable SQL logging that may expose sensitive data
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE
